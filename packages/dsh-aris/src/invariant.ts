/**
 * @aimercat/dsh-aris invariant: the package-manifest registration.
 *
 * No runtime invariant: the persona lives in the preset composition, and the
 * host plugin is a reserved no-op mount — there is no event/data relationship
 * owned by this package to check.
 * @module @aimercat/dsh-aris/invariant
 */

export const name = 'aris-invariant'
